# Ejemplo de variables de entorno para producción
# Copia este archivo a /home/<user>/ControlBovinoVFinal/deploy/.env y ajusta valores

SECRET_KEY=CHANGE_ME_TO_A_RANDOM_SECRET
DEBUG=False
ALLOWED_HOSTS=your.domain.com,127.0.0.1
# Ejemplo para Postgres: postgres://USER:PASSWORD@HOST:PORT/NAME
# Si no quieres Postgres, deja DATABASE_URL vacío para usar SQLite (no recomendado en prod)
DATABASE_URL=

# Azure storage (opcional)
AZURE_ACCOUNT_NAME=
AZURE_ACCOUNT_KEY=
AZURE_CONTAINER=staticfiles

# Email (opcional)
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Otras variables necesarias por tu app
# ...
